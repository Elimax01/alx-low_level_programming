#include "main.h"
#include <string.h>

/**
* _strncat -> function to append
* @dest: first par
* @src: second par
* @n: 3rd par
* Return: string
*/
char *_strncat(char *dest, char *src, int n)
{
	strncat(dest, src, n)
	return (dest);
}
